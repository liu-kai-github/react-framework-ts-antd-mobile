import * as React from 'react';

import {PickerView} from 'antd-mobile';

class HOME_PAGE extends React.Component<any, any> {
    render() {
        console.log(PickerView);
        return (<div>HOME_PAGE</div>);
    }
}

export default HOME_PAGE;
